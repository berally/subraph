{
  "name": "berally-pot",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:sepolia": "mustache config/sepolia.json subgraph.template.yaml > subgraph.yaml",
    "prepare:berachain-testnet": "mustache config/berachain-testnet.json subgraph.template.yaml > subgraph.yaml",

    "codegen": "graph codegen",
    "build": "graph build",

    "deploy:pot:sepolia": "graph deploy --node https://api.studio.thegraph.com/deploy/ berally/pot-sepolia",
    "deploy:berachain-testnet": "goldsky subgraph deploy berally-pot-testnet-v2/2.0.1 --path ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "mustache": "^4.2.0"
  }
}
