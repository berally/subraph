{
  "name": "berally-passes",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:sepolia": "mustache config/sepolia.json subgraph.template.yaml > subgraph.yaml",
    "prepare:berachain-testnet": "mustache config/berachain-testnet.json subgraph.template.yaml > subgraph.yaml",

    "codegen": "graph codegen",
    "build": "graph build",

    "deploy:passes:sepolia": "graph deploy --node https://api.studio.thegraph.com/deploy/ berally/passes-sepolia",
    "deploy:passes:berachain-testnet": "goldsky subgraph deploy bartio-berally-passes/1.0.0 --path ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "mustache": "^4.2.0"
  }
}
