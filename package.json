{
  "name": "berally-passes",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:berachain-testnet": "mustache config/berachain-testnet.json subgraph.template.yaml > subgraph.yaml",
    "codegen": "graph codegen",
    "build": "graph build",

    "deploy:rewards:testnet": "goldsky subgraph deploy testnet-rewards/1.0.0 --path ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "mustache": "^4.2.0"
  }
}
