specVersion: 1.0.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: BerallyPasses
    network: {{ network }}
    source:
      address: "{{ address }}"
      abi: BerallyPasses
      startBlock: {{ startBlock }}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Initialized
        - ManagerFeePercentageChanged
        - ReferralFeePercentageChanged
        - OwnershipTransferred
        - ProtocolFeePercentageChanged
        - TransferFeeChanged
        - Trade
        - Approval
        - Transfer
      abis:
        - name: BerallyPasses
          file: ./abis/BerallyPasses.json
      eventHandlers:
        - event: Initialized(uint8)
          handler: handleInitialized
        - event: ManagerFeePercentageChanged(uint256)
          handler: handleManagerFeePercentageChanged
        - event: ReferralFeePercentageChanged(uint256)
          handler: handleReferralFeePercentageChanged
        - event: TransferFeeChanged(uint256)
          handler: handleTransferFeeChanged
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
        - event: ProtocolFeePercentageChanged(uint256)
          handler: handleProtocolFeePercentageChanged
        - event: Trade(address,address,bool,uint256,uint256,uint256,uint256,address,uint256,uint256,uint256)
          handler: handleTrade
        - event: Approval(address,address,address,uint256)
          handler: handleApproval
        - event: Transfer(address,address,address,uint256)
          handler: handleTransfer
      file: ./src/berally-passes.ts
